import React from 'react';
import { Box, Typography } from '@mui/material';

const DeviceSearch = () => {
  return (
    <Box>
      <Typography variant="h4">DeviceSearch</Typography>
    </Box>
  );
};

export default DeviceSearch;
