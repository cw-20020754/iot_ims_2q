import React from "react";
import { Box, Typography } from "@mui/material";

const DevIntegratedMonitor = () => {
  return (
    <Box>
      <Typography variant="h4">DevIntegratedMonitor</Typography>
    </Box>
  );
};

export default DevIntegratedMonitor;
