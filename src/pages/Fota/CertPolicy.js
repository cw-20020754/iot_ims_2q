import React from 'react';
import { Box, Typography } from '@mui/material';
import fotaStyles from './FotaStyle';

const CertPolicy = () => {
  const classes = fotaStyles();

  return <Typography>certPolicy</Typography>;
};

export default CertPolicy;
