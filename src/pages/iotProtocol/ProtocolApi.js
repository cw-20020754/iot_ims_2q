import React from 'react';
import { Box, Typography } from '@mui/material';

const ProtocolApi = () => {
  return (
    <Box>
      <Typography>ProtocolApi</Typography>
    </Box>
  );
};

export default ProtocolApi;
