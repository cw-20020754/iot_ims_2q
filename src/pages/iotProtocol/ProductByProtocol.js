import React from 'react';
import { Box, Typography } from '@mui/material';

const ProductByProtocol = () => {
  return (
    <Box>
      <Typography>제품별 프로토콜</Typography>
    </Box>
  );
};

export default ProductByProtocol;
