import React from 'react';
import { Box, Typography } from '@mui/material';

const ProtocolFeatures = () => {
  return (
    <Box>
      <Typography>ProtocolFeatures</Typography>
    </Box>
  );
};

export default ProtocolFeatures;
